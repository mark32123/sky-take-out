<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert order_detail (order_id, dish_id, setmeal_id, dish_flavor, number, amount, image, name)
               values
               <foreach collection="orderDetailList" item="odl" separator=",">
                   (#{odl.orderId}, #{odl.dishId}, #{odl.setmealId}, #{odl.dishFlavor}, #{odl.number}, #{odl.amount}, #{odl.image}, #{odl.name})
               </foreach>
    </insert>
</mapper>
